---
- name: Disable password authentication in ssh_config (real disable password auth)
  lineinfile:
    path: /etc/ssh/ssh_config
    regexp: "^(#| )*PasswordAuthentication"
    line: "PasswordAuthentication no"

- name: Disable password authentication in sshd_config (required in task)
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: "^(#| )*PasswordAuthentication"
    line: "PasswordAuthentication no"

- name: Restart SSH service
  service:
    name: ssh
    state: restarted
