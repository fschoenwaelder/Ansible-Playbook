To run the playbook:

1. Install Ansible on your machine.
2. Make sure you have VirtualBox installed with Ubuntu 20.04 LTS and ansible.
3. Open a terminal and navigate to the directory containing the ansible-playbook.
4. Run the following command in the terminal: 
    
    ansible-playbook -i hosts.ini playbook.yml